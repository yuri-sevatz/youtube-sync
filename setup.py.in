#!/usr/bin/env python

from distutils.core import setup

setup(
    name='youtube_sync',
    version='${PACKAGE_VERSION}',
    package_dir={'': '${CMAKE_CURRENT_SOURCE_DIR}'},
    packages=['youtube_sync'],
    scripts=['${CMAKE_CURRENT_SOURCE_DIR}/youtube-sync'],
)
